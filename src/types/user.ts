export type AuthData = {
  name: string;
  avatarUrl: string;
  isPro: boolean;
  email: string;
  token: string;
};

export type UserData = {
  email: string;
};

export type LoginData = {
  email: string;
  password: string;
};
